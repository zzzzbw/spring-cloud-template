spring:
  profiles:
    active: native
  cloud:
    config:
      uri: http://localhost:8888
      name: multi-consumer
      profile: dev
swagger:
  enable: true
  basePackage: cn.zzzzbw.consumer.controller
