spring:
  profiles:
    active: native
  cloud:
    config:
      uri: http://localhost:8888
      name: multi-provider
      profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloudtemplate?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: Zbw19950707
swagger:
  enable: true
  basePackage: cn.zzzzbw.provider.bootstrap.controller
  title: multi provider api
  description: multi provider api create by swagger
