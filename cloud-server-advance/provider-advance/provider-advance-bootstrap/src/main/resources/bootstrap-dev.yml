spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: provider-advance
      profile: dev
swagger:
  enable: true
  basePackage: cn.zzzzbw.server.provider.bootstrap.controller
  title: provider advance api
  description: provider advance api create by swagger
